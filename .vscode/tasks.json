{
  "tasks": [
    {
      "label": "Build camera_calibration",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "build",
        "--target",
        "camera_calibration",
      ],
      "options": {
        "cwd": "${workspaceFolder}/"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ],
  "version": "2.0.0"
}
